# 🏦 Alpha Bank Income Prediction Service

Сервис прогнозирования доходов клиентов на основе ML-модели (CatBoost). Разработан командой **"Титаник 2"** для хакатона Альфа-Банка 2025.

![App Screenshot](https://via.placeholder.com/1200x600.png?text=App+Screenshot+Here)

## 🚀 Возможности

- **Поиск клиента:** Мгновенный поиск по ID с автодополнением.
- **Калькулятор:** Прогноз дохода "на лету" по 200+ параметрам (Real-time Inference).
- **XAI (Explainable AI):** Графики SHAP значений, объясняющие, *почему* модель выдала такой прогноз.
- **Персонализация:** Автоматическая генерация предложений банковских продуктов на основе дохода и поведенческих факторов.

## 🛠 Технологический стек

- **Frontend:** React 19, Vite, Chart.js, Lucide Icons.
- **Backend:** Python 3.10+, FastAPI, Uvicorn.
- **ML Core:** CatBoost, SHAP, Pandas, Scikit-learn.

## 📂 Структура проекта

```bash
/
├── 📂 backend/              # API Сервер (FastAPI)
│   ├── main.py             # Точка входа, роутинг, логика
│   ├── defaults.json       # Значения по умолчанию для калькулятора
│   └── requirements.txt    # Python зависимости
│
├── 📂 frontend/             # Клиентское приложение (React)
│   ├── src/
│   │   ├── App.jsx         # Основной компонент
│   │   └── components/     # UI компоненты (Калькулятор, Графики...)
│   └── package.json
│
├── 📂 ml/                   # Машинное обучение
│   ├── 01_data_preprocessing.py   # Очистка данных
│   ├── 02_model_training.py       # Обучение CatBoost
│   ├── 03_shap_analysis.py        # Анализ факторов
│   ├── 04_generate_recommendations.py # Бизнес-логика
│   └── 📂 data/             # Данные и модели
│       ├── 📂 raw/          # Исходные CSV (в .gitignore)
│       ├── 📂 processed/    # Обработанные данные для API
│       └── 📂 models/       # Обученные .cbm модели
│
└── run_backend.sh          # Скрипт быстрого запуска
```

---

## ⚡️ Быстрый старт

### 1. Предварительные требования
- Node.js 18+
- Python 3.10+

### 2. Установка и запуск

#### Шаг 1: Запуск Backend (Сервер + ML)
Откройте терминал в корне проекта:

```bash
# Создаст venv, установит зависимости и запустит сервер
./run_backend.sh
```
*Сервер запустится на [http://localhost:8000](http://localhost:8000)*

#### Шаг 2: Запуск Frontend (Интерфейс)
Откройте **второй** терминал:

```bash
cd frontend
npm install
npm run dev
```
*Сайт откроется на [http://localhost:5173](http://localhost:5173)*

---

## 🧠 ML Pipeline

Проект включает полный цикл обучения модели. Если вы хотите переобучить модель на новых данных:

1. Положите новые `train.csv` и `test.csv` в `ml/data/raw/`.
2. Запустите скрипты по порядку:

```bash
# Из корня проекта (с активированным venv)
python ml/01_data_preprocessing.py      # Подготовка данных
python ml/02_model_training.py          # Обучение CatBoost (5 фолдов)
python ml/03_shap_analysis.py           # Расчет важности признаков
python ml/04_generate_recommendations.py # Генерация офферов
```

## 🔧 Разработка

### Конфигурация Backend
Для изменения порта или хоста редактируйте `backend/main.py`:
```python
uvicorn.run(app, host="0.0.0.0", port=8000)
```

### Конфигурация Frontend
API URL задается в `frontend/src/App.jsx`:
```javascript
const API_URL = 'http://localhost:8000/api';
```

---

Made with ❤️ by **Titanic 2**
